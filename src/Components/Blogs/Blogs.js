import React from 'react';
import { Link } from 'react-router-dom';

const Blogs = () => {
    return (
        <Link to='/blogs'>
            <h1>This is blogs</h1>
        </Link>
    );
};

export default Blogs;