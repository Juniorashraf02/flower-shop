import React from 'react';
import { Link } from 'react-router-dom';

const dashboard = () => {
    return (
        <Link to='/dashboard'>
            <h1>dashboard</h1>
        </Link>
    );
};

export default dashboard;